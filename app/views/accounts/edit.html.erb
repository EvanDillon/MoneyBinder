<h1>Editing Account</h1>
<%= render 'form', account: @account %>
<%= button_to "Delete", @account, method: :delete, class: "btn btn-danger", data: { confirm: 'Are you sure?' } %>
<%= link_to 'Back', accounts_path %>